# Shell scripts must stay LF — a CRLF deploy.sh dies on Linux with
# "bad interpreter: /usr/bin/env bash^M". Authored on Windows, run on Ubuntu.
*.sh text eol=lf

# Windows-only helper scripts
*.ps1 text eol=crlf
